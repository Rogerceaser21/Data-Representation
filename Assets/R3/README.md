# R3 Evidence Form

Digital R3 evidence form for SPEA / governance observations at AIS Sharjah. Same architecture as the lesson observation form: HTML submits to an Apps Script web app, which appends a row to a Google Sheet, and emails an HTML backup copy with a per-record locked-view URL to `admin.user@ais.ae` and the selected inspector.

Currently at **v0.34**. From v0.29 the live form is **password-gated with StatiCrypt** and locked records require a per-record token. From v0.34 Tom Select uses **opaque indices** as option values to sidestep an iOS WebKit bug that was blanking dropdowns on iPad Safari + iPad Chrome.

## Live URL + access password

| Thing | Value |
|---|---|
| Live form | https://rogerceaser21.github.io/Data-Representation/Assets/R3/r3-evidence-form.html |
| Access password | `ais2026ais` (school-wide gate) |
| Locked-record URL pattern | `r3-evidence-form.html?id=AIS-R3-YYYYMMDD-HHMM&token=<32-hex>` (token comes from the submission backup email) |

Google file links: see [Google file links.md](Google%20file%20links.md).

## Folder layout (v0.34)

```
Assets/R3/
├── src/r3-evidence-form.html         # MASTER source · edit this
├── r3-evidence-form.html             # ENCRYPTED output · GENERATED by encrypt.sh, do not hand-edit
├── password-template.html            # AIS-themed StatiCrypt gate (dark / light, AIS yellow, prewarm)
├── encrypt.sh                        # one-shot: encrypts src/ → root with password "ais2026ais"
├── .staticrypt.json                  # encryption salt (commit OK; not a secret on its own)
├── lib/                              # vendored Tom Select (used post-decrypt)
├── apps-script/                      # clasp project for the backend
├── R3 Evidence Form Template.docx    # SPEA source template
├── Google file links.md              # quick-access Sheet + Apps Script + Gmail links
├── 2026-05-28_email_R3-Data_to-Dave_cc-Leon-Steve.md
└── README.md                         # this file
```

## Edit / deploy workflow

1. Edit `src/r3-evidence-form.html` (the master).
2. Bump the version footer inside the master (and in the Lesson Observation form, to keep the project-wide stamp in sync).
3. Run `./encrypt.sh` from this folder. It produces a fresh `r3-evidence-form.html` (the encrypted output).
4. Commit, tag, push: `git add ... && git commit -m "vX.Y · ..." && git tag vX.Y && git push origin main --tags`.
5. Force a Pages rebuild: `gh api -X POST repos/Rogerceaser21/Data-Representation/pages/builds`.

To rotate the school-wide password: edit `PASSWORD=` in `encrypt.sh`, re-encrypt, commit.

## Apps Script + Sheet

Backend is the `apps-script/` clasp project (running as `admin.user@ais.ae`). On submit:

- Append a row to the `Submissions` tab (48 columns; last is `record_token`).
- Generate a 32-char hex token, stored alongside the record.
- Compute `duration` server-side as integer minutes from `time_in` and `time_out` (v0.32).
- Email `admin.user@ais.ae` an HTML summary + the locked-view URL with token; CC the selected inspector if their `Inspectors!B` email cell is populated (v0.31).

To redeploy after a backend edit (URL stays stable):

```bash
cd Assets/R3/apps-script
clasp push --force
clasp redeploy AKfycbx3efKiQzs2MSwESEuNBCceXr5FqBCXuk1IgSzPFbOVgLSc3fvXy40e8V9lhw_KH0z2nQ -d "vX.Y summary"
```

## Cache invalidation (v0.33)

`02_doGet.gs` `getDropdownOptions()` is wrapped in `CacheService` with a 5-minute TTL. If you edit the Teachers / Inspectors / Curriculum / Subjects tabs and need the form to see the change before the TTL expires, run `clearOptionsCache` from the Apps Script editor's function dropdown. Otherwise the form serves the cached options until natural expiry.

## Gmail filter (one-time)

Set up once in Gmail UI on the `admin.user@ais.ae` mailbox so submission emails auto-archive into a labelled folder:

> From: `admin.user@ais.ae`  
> Subject contains: `AIS R3 Evidence ·`  
> → Skip Inbox, Apply label `R3 Submissions`.

After the first deploy with the `script.send_mail` scope, open the script in the browser editor and run `forceAuth` once to grant the email permission.

## Changelog · v0.30-v0.34

| Tag | What changed |
|---|---|
| v0.30 | Gate pre-warms Apps Script with a background `fetch('?action=options')` so the form's first fetch lands on a hot runtime. Gate now uses the same `localStorage` theme key (`ais-form-theme`) as the form so a dark/light toggle on the gate carries into the form. |
| v0.31 | Frosted-glass loading overlay over the form while options are fetching. Defensive `safeForSelector` server-side normalisation: substitute straight apostrophes with curly U+2019 and straight quotes with curly U+201D in everything returned by `?action=options`. Inspector CC on the backup email (looked up from new `Inspectors!B email` column). |
| v0.32 | Form layout reflow: Teacher (span-2) + Time In on row 1; drop Duration field from UI; Room Number moves to row 3 col 2; Ability Group + Gender Mix move to row 4. Time In + Time Out now mandatory. Time Out moved to bottom, beside Summary of Weakness. Auto-compute duration server-side in `01_doPost.gs` as integer minutes. iPad date/time inputs get a visible dashed underline so users see a tap target. |
| v0.33 | `CacheService` wraps `getDropdownOptions()` with 5-min TTL; `clearOptionsCache` helper added. Row 1 reflows to Teacher \| Support Teachers / CAs \| Time In (3 equal-width cells). Time Out width shrunk to ~1/6 of form. Email locked-URL callout drops "FORWARD TO TEACHER" and hides the raw URL behind a clean "Link" anchor. |
| v0.34 | iOS WebKit DOMException "The string did not match the expected pattern" fixed by using **opaque indices** (`t0`, `i0`, `s0`...) as Tom Select option values for teacher / inspector / subject. Display text holds the human name. `submitForm` + `saveForm` translate value → text before write so the Sheet still stores names. `loadClosedRecord` + `populateTomSelect` look up options by text for restoring saved state. **See hard rule 11 in `../../CLAUDE.md`.** |

## Full project context

See `../../CLAUDE.md` (project root) for:
- Architecture diagram (forms ↔ Apps Script ↔ Sheets)
- All IDs (scriptId, /exec URL, Sheet IDs)
- Hard rules (column schema is load-bearing, record ID format, opaque Tom Select values)
- Identity / auth context (everything under admin.user@ais.ae)
