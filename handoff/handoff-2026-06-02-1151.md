# Handoff · Data-Representation deck (next session)

**Written:** 2026-06-02 11:51
**By:** prior Claude session (Opus 4.7)
**For:** fresh agent picking up Igor's Data-Representation work
**User:** Igor Sesar, AIS Sharjah (igor.sesar@ais.ae)
**Project root:** `/Users/igor/Library/CloudStorage/GoogleDrive-igor.sesar@ais.ae/My Drive/Digitech/Claude Igor Online/Data-Representation/`

---

## What this session was for

This session spanned several days of work on a teacher observation pitch deck and associated forms going to AIS Sharjah principal Steven McLuckie. Work covered:

1. Recovery / documentation of an existing v0.8 system (created `CLAUDE.md` + memory)
2. Migration of Apps Script from a personal Gmail to admin.user@ais.ae (v0.9)
3. Build of a second HTML form (R3 Evidence Form) with its own Sheet + Apps Script (v0.10)
4. Two failed attempts to fix a slide content clipping bug (v0.11, v0.12), both of which made things worse
5. Reverted the changes the prior agent made to the `frontend-slides` skill
6. Installed mattpocock/skills bundle (29 skills) including this `handoff` skill

The session ended with the deck visually broken at v0.12. Igor explicitly handed off because he lost trust in the prior agent's judgment.

---

## Read these in this order before doing anything

1. **`<project root>/handoff.html`** · The comprehensive handoff brief the prior agent wrote earlier today. Contains embedded screenshots of the broken v0.12 state, root-cause analysis, file paths, all IDs, bug timeline, list of what works vs what doesn't. **Read this first; do not duplicate its content here.**
2. **`<project root>/CLAUDE.md`** · Project bootstrap doc. File map, hard rules, deploy chain, identity / auth (everything under admin.user@ais.ae), karpathy discipline, em-dash prohibition. **Hard rules are load-bearing; don't break them.**
3. **`<project root>/.planning/v0.10-r3-form-checklist.md`** · The R3 Evidence Form build plan (complete). Useful context for how that piece works.
4. **`<project root>/Assets/R3/README.md`** · R3 folder map + the three R3 links (form live URL, form source on GitHub, R3 Sheet URL).
5. **`~/.claude/projects/-Users-igor-Library-CloudStorage-GoogleDrive-igor-sesar-ais-ae-My-Drive-Digitech-Claude-Igor-Online-Data-Representation/memory/MEMORY.md`** · Index of project memory. Especially `feedback_no_em_dashes.md`, `feedback_broaden_scope_for_systemic_bugs.md`, `feedback_karpathy_discipline.md`.
6. **`/Users/igor/.claude/plans/picture-1-100-frolicking-parasol.md`** · The plan file from today's failed fix attempts. Useful for understanding what was tried and rejected.

---

## Current git state (as of handoff)

Last few commits (full log: `git log --oneline -10` from project root):

- `v0.12` (BROKEN) · attempted fix that releases inner overflow:hidden; created overlapping content + page chrome
- `v0.11` (BROKEN) · attempted fix via JS observer + `.has-overflow` CSS; observer's measurement defeated by inner overflow:hidden
- `v0.10` · last known good state for slides 1, 2, 5 (slides 3, 4 still had the original silent-clipping bug at MacBook 100% zoom)

Pages auto-rebuild is flaky on this repo. Force rebuild after push with `gh api -X POST repos/Rogerceaser21/Data-Representation/pages/builds`. Large pushes may need `git config http.postBuffer 524288000`.

---

## Critical caveat · verification

The prior agent's two failed attempts both passed headless Chrome diagnostics at `--window-size=1440,900` because headless has no browser UI chrome, so the effective viewport is the full 900px and content fit. Real Chrome on Igor's MacBook at 100% zoom has ~120px of browser UI chrome, leaving ~780px effective viewport, where the bug manifests.

**Do not declare a fix working until you have verified visually in conditions matching Igor's real setup.** The screenshots Igor pasted (which the prior agent failed to inspect carefully) showed clear visual problems even at the viewport the agent used. Look at every screenshot before shipping.

**Slide 1 distortion does NOT reproduce in headless Chrome at 1440x800.** It does reproduce in Igor's real Chrome. Before diagnosing slide 1, get Igor's real `window.innerWidth × window.innerHeight × devicePixelRatio` (he can read this from devtools console).

---

## What is NOT broken (don't touch)

Per the more detailed list in `handoff.html`:
- Both forms (Lesson Observation form, R3 Evidence form) work end to end.
- Both Apps Script backends + both Sheets work.
- PDF export captures full content on every slide (the print CSS that was added in v0.12 actually works correctly; that's the one thing v0.12 got right).
- The `frontend-slides` skill at `~/.claude/skills/frontend-slides/` was reverted to its original pre-this-session state. Three files were touched (`viewport-base.css`, `SKILL.md`, `html-template.md`) and all reverted cleanly. Verified 0 residue matches for the prior agent's additions.

---

## Suggested skills for next session

These are now available globally on this machine (installed today from `mattpocock/skills` plus existing skills):

- **`/diagnose`** (mattpocock) · for systematic root-cause analysis of the slide clipping + layout-overlap bug. The prior agent jumped to "I see the cause" twice and was wrong both times. Slow down with this skill.
- **`/zoom-out`** (mattpocock) · could be useful for stepping back from the broken-fix state and re-examining the layout architecture rather than patching at the leaves.
- **`/grill-me`** or **`/grill-with-docs`** (mattpocock) · could be helpful for Igor or the next agent to challenge assumptions before coding.
- **`karpathy-guidelines`** (pre-existing) · four principles already baked into the project's CLAUDE.md (think before coding, simplicity first, surgical changes, goal-driven execution). Re-invoke on bootup.
- **`frontend-slides`** (pre-existing) · the skill the deck was generated from. The prior agent broke + reverted it. If you generate any new decks, use this skill but be aware of its `Viewport Fitting (NON-NEGOTIABLE)` rule (the entire bug Igor hit yesterday is a consequence of that rule).
- **NOT recommended:** `frontend-design`, `ui-ux-pro-max`. These are good for general UI work but the current bug is a specific layout-architecture mismatch that needs targeted debugging, not a redesign.

---

## Igor's communication preferences (read before responding to him)

Captured in memory at `~/.claude/projects/.../memory/` (see `MEMORY.md` index). Key rules:

- **Never use em dashes (—) or en dashes (–) in any output.** Substitute with commas, periods, parens, semicolons, or sentence breaks. Hyphens in compound words ("spreadsheet-first", "Sheets-first") are fine.
- **When fixing a bug, broaden scope to the shared layer if the bug pattern is systemic.** Igor explicitly rejected a narrow fix today.
- **Plan before coding.** Igor's phrasing: "PLEASE TELL ME WHAT YOU THINK I WANT YOU TO DO. TELL ME HOW YOU PLAN TO IMPLEMENT THIS. AWAIT MY APPROVAL BEFORE YOU DO ANY CODING." When he says this, plan and ask, do not code.
- **Verify before declaring done.** The prior agent shipped two broken versions because it skipped real-browser verification. This is the main reason Igor lost trust.
- **Don't relitigate decisions.** Once he has made a call, execute. Don't re-raise the same concern with different framing.
- **Don't over-explain.** Concise answers. Tables over paragraphs when content allows.

---

## Open questions for Igor (ask before code changes)

1. **Real-Chrome viewport for slide 1 distortion?** What's `window.innerWidth × window.innerHeight × devicePixelRatio` when the slide 1 bug is visible? Required before any slide 1 fix.
2. **Approach preference for the slide overflow problem?** (a) preserve "matrix fills slide" aesthetic with internal-container scrolling, or (b) shift to "matrix natural size, slide scrolls as a whole" (sparse slides get empty space at bottom). The prior agent did not get an explicit answer.
3. **Revert v0.11 and v0.12 first, or work forward from current state?** v0.10 had the original silent-clipping bug but did not have the overlap-with-page-chrome bug.
4. **Should the deck go to Steven McLuckie next week as planned, or is the timeline now pushed?** Affects how aggressive the fix needs to be.

---

## Sensitive data note

This handoff intentionally contains the project's email addresses, scriptIds, Sheet IDs, and `/exec` URLs because they are necessary for the next agent to function on this project AND they are already present in the committed `CLAUDE.md` at the project root. They are not "secret" relative to this work. No passwords or API keys are included.
