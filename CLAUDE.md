# CLAUDE.md · Data-Representation

> Bootstrap doc for future Claude sessions. Read this *before* touching anything in this folder.

---

## What this is

AIS Sharjah **Teacher Observation showcase system**, pitched 2026-05-21 to principal Steven McLuckie ahead of the early-2027 school review. Currently at **v0.50** with **two parallel HTML forms** (plus an ungated teacher viewer generated from the R3 master) wired to **two separate Google Sheets** backed by **two separate Apps Script projects** (both under admin.user@ais.ae). **v0.47 added the R3 -> Supabase dual-write bridge**: every R3 submission now lands in BOTH the Google Sheet (source of truth) AND the dashboard's Supabase, plus a password-gated admin Settings cog on the R3 form that sets the current inspection Round (see hard rule 14). **v0.50 added the Evidence Pad** to the R3 form: a full-screen iPad scribble pad (camera/library photos + Apple Pencil pressure ink) whose handwriting is extracted by Claude vision into the form's free-text fields behind a review step; the pad itself is stored in a PRIVATE Supabase bucket and attached to the backup email (see hard rule 15).

1. **Lesson Observation form**, internal AIS template, 4 sections, 36 S/U criteria, signatures, OTP aligned
2. **R3 Evidence form**, SPEA / governance template, single-select Evidence Type, 10 judgements on a 1-6 scale, searchable teacher dropdown sourced from 6 staff Workspace Groups

Plus a **5-slide HTML pitch deck** at `index.html` that links to both forms from P3.

Live: https://rogerceaser21.github.io/Data-Representation/

---

## Dashboard build (Supabase) · active workstream, separate from the forms/deck

A role-based **Supabase-backed dashboard** is being built to aggregate all teacher-assessment data for the principal, governors, and the SEF/SEAS review. It is not the forms and not the deck.
- **BUILT + DEPLOYED (gated), iterated to v0.60 (2026-07-05); now reads LIVE from Supabase** in `dashboard/` (tag `dash-v0.60`). Live, StatiCrypt-gated (password `ais2026ais`): https://rogerceaser21.github.io/Data-Representation/dashboard/ . Snapshot board (landing) + Coverage + Observations drill-down + Settings; Cinematic shell, light default + dark, AIS star-field. **Scoring (final, do not regress): per observation = best skill (MIN); teacher overall = the average of those per-observation scores** (sessions 6,6,5,5 -> 5.5). v0.7 reworked the Teacher-board session card (approved preview `dashboard-previews/card-A-revised.html`): two collapsible titled columns (Inspection details, Skills observed), full-width Spectrum, a condensed action bar (observed teacher name + centred Observation Notes toggle + text-only link to the locked R3 record, June submissions only since February has no token), and an expander for notes/strengths/areas-to-develop. The snapshot now carries notes/strengths/weakness/source/token. v0.8 refined the card: any click selects it; the Inspection/Skills arrows sit beside their titles, bob, and open BOTH columns together; the session-card Spectrum is a full-width HTML/CSS bar at a fixed thin height (`specHTML`/`animSpec`; a width-scaled SVG read as fat) with judgement words at the body-text size; the column headers match the Observation Notes header. **v0.9 removed the two top centerpiece cards** and merged the Teacher-board top graphs into the All-observations card: the Spectrum and the old All bar were the SAME data (one dot per observation at its best score), so they are now one full-width HTML/CSS Spectrum (`specHTML`, score-coloured dots + average flag + selected-session ring) inside the All card, with the gauge beside it. The gauge is renamed **Student Progress** and shows ONLY the Progress judgement (criterion `r3_02`): rim dots are each observation's Progress score, the fill/word is the teacher-wide average (`progScore`/`progAvg`); it is built once in `renderTeacher` and **never recalculated on session select** (always reads ALL). Selecting a session only rings its dot on the Spectrum; the gauge is untouched. `buildSpec`/`buildObsRamp`/`SEL_COLOR` and the dead `.tcard`/`.cpstage`/`.oramp` CSS were removed. Individual session cards unchanged. **v0.10** tidied the All card (the inspector legend and the Average-of-observations word removed, the Student Progress label bold, the gauge pulled up beside the Spectrum) AND fixed the freeze bug *properly*: v0.9's GSAP-from-invisible entrances plus a `finalizeViz` rescue could still strand the dots/gauge blank after a backgrounded/bfcached tab, so the Spectrum (`.ospec`/`.osbar`) and gauge (`#svgArc`) entrances are now CSS keyframe animations on a resting-visible base. CSS animations run on the real-time document timeline (NOT requestAnimationFrame), so they always complete to the final visible state whether or not the tab was painted; `animSpec` and `finalizeViz` were removed. **Do not reintroduce a from-invisible (`scale:0`/`opacity:0`) JS entrance on these viz elements; keep the resting state visible.** **v0.11** balanced the All card: "All observations" and "Student Progress" are now matched bold headers (`.ey-bold`, in `.allhead`); the gauge `viewBox` is cropped+centred on the circle and `.allmain` uses `align-items:center` so the gauge circle's middle lines up with the Spectrum bar's middle (the Spectrum dots are absolutely positioned, so the bar never moves and the alignment holds regardless of dot stacking); the card height is trimmed. **v0.12-v0.21 (2026-06-18):** v0.13 made the All card symmetric with a **Teaching gauge** (criterion `r3_05`, mirror of the Progress gauge) right of the Spectrum (`buildArc(t,svgId,scoreFn,avgFn,mirror)`); v0.14-v0.15 restyled the All-card Spectrum as the **SEAS scale** (one seamless bar, six swatches + words, no numbers, dots reserve height, bottom-aligned with the gauges; a `segmented` flag on `specHTML` scopes it to the All card, session cards keep the gradient bar); v0.16-v0.17 renamed **Teacher -> Observations** and moved the teacher search off Coverage; v0.18 added **hash routing** (`#teachers`/`#settings`); v0.19 made **R3 Obs a fan-out menu** (Teacher Level = the working search; Grade/Department + School Level = front-end placeholders); v0.20 removed the masthead Stars/theme buttons (kept in Settings) + added a floating top-right sun/moon theme toggle; **v0.21 wired LIVE Supabase read** (see build chain). **v0.22 (2026-06-19) added the Snapshot board, now the LANDING view** (`#v-snapshot`, first pill, default no-hash route; Coverage moved to `#coverage`): a governance at-a-glance of the June R3 sweep (`SNAP_ROUND='R3 June 26'`, scoped to that one round) with a **Primary & Kindy / Secondary / Compare** toggle (`snapPhase`). Per phase it shows lessons observed, teachers seen, average progress (best Progress `r3_02` per teacher then averaged across teachers, best-evidence-wins) on a ramp gauge + a six-point distribution histogram, a What-is-working / Where-to-focus-next summary, and **three insights each** (the baked `NARR` constant, a human synthesis of the round's written strengths + areas-to-develop); Compare adds a diverging PK-vs-Secondary distribution chart + three cross-phase insights. All numbers compute LIVE from `D` via `snapAgg()` (works on baked + live). **`teachers.section` (subschool: kindy/primary/secondary) is now POPULATED in Supabase** from the R3 Sheet Teachers tab (Kindy/Primary/Secondary booleans) via the re-runnable tool `~/AIS-Data-Dashboard/db/set_sections.mjs`, so the phase split is live data, not hard-coded; the export/RPC/`shapeSnapshot` already carried `section`, it was just unpopulated. `data.js` rebaked to match. Verified end-to-end in a browser (both themes, all three modes, through the StatiCrypt gate; numbers verified vs SQL). **v0.23 (2026-06-19) corrected the Snapshot to count distinct LESSONS, not raw form submissions:** most June lessons were co-observed by two inspectors who each filed a form, so the raw 167 rows over-counted. `snapAgg` now collapses a teacher's June submissions into lessons by teacher + date + normalised subject (`lessonSubjKey` via `resolveArea`, folding variants like "Music"/"QCE Music In Practice"), best score winning (min) across co-observers. Corrected counts: overall **109 lessons / 103 teachers**, Primary & Kindy **47/45**, Secondary **62/58**; Secondary **Very Weak = 4** (the v0.22 board showed 8, which was 4 lessons each filed by two inspectors), Outstanding 6. Average ratings + teacher counts are UNCHANGED (best-per-teacher is invariant to duplicates: PK 3.19, Secondary 3.24). The dedupe is ISOLATED to the Snapshot (`snapAgg` + `lessonSubjKey`; it READS but never mutates `D.teachers[].observations`), so per Igor's instruction the **Observations board is untouched** and Coverage is unchanged; a `snapReady` gate fixes the load-order TDZ introduced by `snapAgg` now using `resolveArea`. tag `dash-v0.23`. **v0.24 (2026-06-19) made the Snapshot an animated STORY by default** (the Snapshot is also now the dashboard LANDING page; login always lands there, stale hash cleared). The Snapshot has a **Story | Detail** toggle (`snapView`): Story is a beat-by-beat narrative on a `.stage` (cover, scale with custom Teacher figure-cluster + Observations lens SVGs, progress gauge+distribution, what-is-working bullets + insights, where-to-focus bullets + insights; Compare has its own 4 beats ending on the diverging chart), built to the `look-cinematic.html` motion grammar via a slide engine (`renderStory`/`gotoBeat`/`animateBeat`/`phaseBeats`/`compareBeats`); it **auto-plays with play/pause, prev/next + arrow keys, beat dots, full-screen Present, and a persisted Auto-play on/off switch in Settings** (`ais-snap-autoplay`). **Detail is the PREVIOUS card view, kept verbatim as v1**, one click away (`renderDetail`). Per Igor: this is all isolated to the Snapshot (the slide engine reads but never mutates `D.teachers[].observations`); **Coverage and Observations stay untouched, and Coverage is "designed, do not touch".** rAF-safe (active beat shown via a CSS `.on` class at final state; entrances are `immediateRender:false` + `settle()`). **v0.25 (2026-06-20) rebuilt the Story so the DATA carries the animation, not illustrations** (Igor rejected v0.24's hand-drawn SVGs and invented narration). Removed the teacher-cluster + eye SVGs. Hero motion is now a **SEAS dot-sort** (`renderDotSort`): each lesson is a dot that flies in (CSS keyframes, rAF-safe) and sorts onto the six-point scale by colour, beside the gauge sweep; the numbers beat is big count-ups; the cover uses a generated AIS-toned image (`dashboard/assets/snapshot-cover.jpg`, public, non-sensitive). **The beats use ONLY the V1 (Detail) wording + stats verbatim** (the V1 section labels, evidence lines, summaries, bullets, insights), no invented copy. Beats per phase: cover, numbers, student-progress (dot-sort + gauge), what-is-working bullets + its insights, where-to-focus bullets + its insights; Compare: cover, numbers, distribution (diverging), insights. Controls + Detail(v1) + scope unchanged from v0.24. Built consulting the **frontend-slides** skill. (Note: DesignSync/"design-sync" is a publish-to-claude.ai/design sync tool, NOT a design generator; the look is hand-built to dashboard-aesthetic. Bespoke decorative SVG illustration is a known weak spot, hence the dot-sort + generated image approach.) **v0.26 (2026-06-21) restyled the Story to Igor's illustrated Claude-design rework** (stitched into the master, wired to LIVE data): a navy classroom cover, recoloured pencil-PNG motifs on the numbers tiles, quiet margin figures on the statement + insight beats, drifting motes, a per-beat progress bar, and big colour-coded presenter headers. It uses ONLY the live `snapAgg(phase)` numbers (the standalone's frozen `AGG` was dropped) + the existing baked `NARR` wording (V1 verbatim); the dot-sort / gauge / diverging data visuals are kept; the gauge stays freeze-safe (no from-empty GSAP sweep). The Fonts link is now `display=block` to kill the serif headline flash. Isolated to the Snapshot Story: Detail (v1), Coverage and Observations untouched. (WIP checkpoint: Igor has further design changes queued for 2026-06-22; moving the insight TEXT into Supabase, vs the baked `NARR`, is a deliberate next step, not done here.) **v0.27 (2026-06-22) fixed two reported Story bugs with one root cause:** the freeze-safety helper `settle()` used `clearProps:'all'`, which wiped the beats' hand-authored inline styles (not just GSAP's props) about 1.9s after each beat, so the `.beat-h` summary jumped from its inline `clamp(18-25px)` to the base 42px (the first-load small-to-big reflow on slides 4/6) and the `.beat-k` kicker reverted from green/terracotta to base blue (the in-play colour change on slides 4-7). It read as first-load-only because the strip is permanent until re-render. Fix: `settle()` now clears ONLY `transform,opacity,filter` (the props GSAP animates), preserving freeze-safety AND the author inline size/colour; NOT a webfont issue (display=block/preload could never fix it). Verified the Story (both themes) + the Observations board (which shares `settle()`) with no console errors (tag `dash-v0.27`). **v0.28 (2026-06-22)** made Story autoplay LOOP (wraps to beat 0 at the end via `scheduleNext`) and fixed the "background the tab, come back, some viz blank, needs refresh" bug (same class as the R3 form bfcache issue, hard rule 12): a new `repaintRestore()` re-renders the CURRENT board at final state on `pageshow`(persisted) + `visibilitychange`->visible, preserving the open teacher + the story position (mirrors `loadLive`'s re-render set; no state reset, no reload). Verified a deliberately blanked teacher gauge repaints on restore (tag `dash-v0.28`). **v0.29 (2026-06-22)** halved the observer figures on the Insights beats (slides 5/7, `.statefig.figtall`) toward the teacher-motif scale, and recoloured the teacher motif PNG from its off-brand #4C82F5 to the approved AIS blue #1257FF ("Blue on Star", from the AIS Approved Colours sheet) so it matches the presenter headers (which were already #1257FF). Story-only (tag `dash-v0.29`). **v0.30 (2026-06-22) shipped #6/#7 (Insights -> Supabase + references):** every Snapshot claim (summaries, bullets, insight cards, in BOTH Story and Detail, all three phases) now shows a small numbered marker that opens a **slide-out panel from the LEFT** with the supporting evidence (one card per source lesson: subject area + date + progress word + the inspector's verbatim quote), a "how this is counted" note where relevant, and a link to that lesson's locked R3 record. User-facing it is **"Insights", never "AI"**, and references NEVER name a teacher (a panel footer says identifying details are withheld). The wording AND the references read LIVE from two new round-scoped Supabase tables `narrative` + `narrative_ref` via the definer RPC `get_narrative(p_round)` (anon-granted, like `get_raw_snapshot`); the baked `NARR` constant stays the instant-first-paint + offline fallback and the markers are progressive enhancement (no Supabase -> baked text, no markers, no error UI). SQL `~/AIS-Data-Dashboard/db/migrate_05_narrative.sql` (additive; applied via `db/apply.mjs`), seeded by `db/seed_narrative.mjs` from the **Phase A congruence audit** (`.planning/2026-06-22-phaseA-congruence-audit.md` + `phaseA-reference-map.json`: all 43 Snapshot claims cross-referenced against the live June R3 evidence, deduped to lessons mirroring `snapAgg`; **39 congruent, 4 FLAGGED with the real number** (the Primary evidence-base denominators drifted 47->48 as late forms kept arriving, last 06-22 12:22; the literal "just one" behaviour note understates ~5 mentions; the Secondary subject-direction is a tendency not absolute) and the wording is **NOT auto-rewritten** per Igor, the flags await his call). Round-scoping also seeds the multi-round selector (#8). Snapshot-only; Coverage + Observations untouched; the panel uses CSS-transition motion (freeze-safe). Verified in-browser both themes, Story + Detail, all phases, zero console errors. tag `dash-v0.30`. **v0.31 (2026-06-23) reworked the sources panel per Igor:** it now opens from the RIGHT (was left) as a **push-drawer** (`body.refopen` reserves `--refw`; the app `.wrap` shifts left so the Story + its sources show at once), the heavy click-blocking scrim is replaced by a **light non-blocking dim** (`.wrap` opacity .84) so markers stay clickable and you can jump source-to-source (close via X / Esc), and the **blue Story presenter headers move to the AIS navy** (`var(--ink)`, matching the masthead title) with the centred-layout beats (numbers, progress) now centring their header (green/terracotta headers unchanged). NB the AIS approved "Blue on Star" is `#1257FF` and the headers already WERE that, so Igor wanted them OFF the electric blue, hence navy. Build gotcha: avoid `calc()` wrapping a `clamp()`-valued var (`padding-right:var(--refw)`, not `calc(var(--refw)+28px)`), the preview engine drops it. Snapshot-only; Coverage + Observations untouched; tag `dash-v0.31`. **v0.32 (2026-06-23) restructured the Snapshot header + Detail Insights** (Igor): header is two columns (`.snaphead`/`.snaphL`/`.snaphR`) = left kicker + timestamp under it, right title + subheading right-aligned to the masthead edge; phase tabs + Story/Detail toggle share one `.tabsrow` (space-between); Detail Insights split into TWO `.panel` cards in `cols-2` (identical to the What-is-working/Where-to-focus pair, `insCol()` now returns a panel with a `.swhead`, green "Insights . Strengths" + terracotta "Insights . Areas to develop"). Snapshot-only; Coverage/Observations/Settings + Story content untouched; tag `dash-v0.32`. **v0.33 (2026-06-23) made the 3 board headers congruent + compact** (Igor; Coverage "do not touch" lifted): removed the Snapshot subheading (`#snapIntro` gone), and **Coverage + Observations now use the same `.snaphead` two-column header** as Snapshot (kicker + timestamp left, title right). **Observations restructured:** title = "R3 Observations" (right), the R3 Obs control + search + filter moved into a `.tabsrow` under the header (where Snapshot's phase tabs sit), and the "Back to Coverage" button removed (element + handler); R3 Obs menu / Teacher search / drill-down unchanged (IDs preserved, verified). tag `dash-v0.33`. **v0.34 (2026-06-23) Snapshot Story polish (tasks 3 & 4):** the Story `.stage` is taller (`clamp(460px,72vh,720px)`, one fixed box for all seven beats) so the two Insights beats (slides 5 & 7) show all three cards without the last clipping; **Present mode now opens in REAL full screen** (`requestFullscreen()` on the document, webkit-prefixed; `fullscreenchange` syncs `.present` back out on Esc; iOS Safari without element-fullscreen silently falls back to the CSS full-window present, hard rule 12), the present stage is **opaque** (`var(--bg)`), **every beat's text + figures are larger and centred** (`#v-snapshot.present` rules re-express the fixed px as `vh`-scaled clamps), and an **A- / A+ text-size control in the Present bar** mirrors the Lesson-Plan slides' `--font-scale` (step 10%, 50-200%, session-only, Present-only; also Cmd . / Cmd ,). All Present-mode; the embedded Story keeps its sizes; Coverage + Observations + Detail untouched. tag `dash-v0.34`. **v0.35 (2026-06-23) Coverage corrected + Print button:** Coverage no longer reads the baked `D.coverage`/`D.quality` (which pooled Feb + June raw submissions); a new `covAgg()` recomputes from `D.teachers[].observations` scoped to `SNAP_ROUND` and deduped to lessons exactly like `snapAgg`, so the boards agree by construction (`covAgg().lessons===snapAgg('all').lessons===110`). New June-only numbers: 149 register, 104 observed (was 135 = 104 June + 31 Feb-only), 45 not-yet (was 14), 110 lessons (was 312 raw); verified at the Supabase source (Feb is a mock seed, June is the live `r3_sheet`). A floating **Print / Save-as-PDF** button (next to the theme toggle, on the 3 data boards, off Settings + Present) prints the current board via `@media print` + `beforeprint`/`afterprint` (works for the button AND Cmd/Ctrl+P): forces light, hides chrome, freezes count-ups; **Story prints every beat one page each** at final state, **card views stack cards with `break-inside:avoid`** so no component splits across a page. tag `dash-v0.35`. **v0.36 (2026-06-23) Print rendering fixes (print-only, dashboard untouched):** all scoped to `@media print` + the print-prep. (1) Browsers strip `background` colours in print and every chart fill is a background, so the bars/dots/swatches/gauges/navy-cover printed blank; added `print-color-adjust:exact` (+ `-webkit-`) and `transition:none` (bars at final size). (2) print-story beats were `position:static`, un-anchoring their absolute cover image + `.statefig` figures (they piled at the page bottom); changed to `position:relative` so each slide's image/figure anchors to its own page; the cover line-art keeps its brightness filter via an id-specific selector that out-specifies the blanket `.beat * filter:none`. (3) Coverage `cols-4` stat tiles now `flex-wrap` + hug their text instead of full-width block. On-screen dashboard byte-for-byte unchanged (verified statgrid still `grid`, no leak). tag `dash-v0.36`. **v0.37 (2026-06-23) Story controls moved into the tabs row (layout only):** the `#storyCtl` pill (prev / play-pause / next / dots / Present) moved out of `#snapStory` (was a bar below the stage) into the `.tabsrow` between `#phaseTabs` and `#viewTabs`, restyled compact (sbtn 38px -> 30px) to match the phase pill; shown only on the Story view (`renderSnapshot` sets `storyCtl.hidden=snapView!=='story'`, since it was auto-hidden inside `#snapStory` before), still floats to the bottom in Present (`position:fixed`) and hidden in print; IDs preserved so all handlers are unchanged. tag `dash-v0.37`. **v0.38 (2026-06-23) Settings: password gate (popup every open) + Supabase persistence:** clicking Settings pops a centered password modal (`#setGate`); `showBoard` intercepts `settings` and opens it only after `verify_admin_password` (the SAME shared admin password as the R3 form, bcrypt in `app_config`); `adminPw` is cleared on leaving Settings so it re-asks every open; Cancel/Esc/dim dismiss. The light/dark toggle was removed from Settings (floating button is the only theme control; theme stays per-device localStorage). **Star-field on/off + the 3 sliders + Snapshot auto-play now persist to Supabase** (written password-gated via `admin_set_setting`, sliders debounced; read via the anon `get_app_settings()` on load), so an admin change applies on all devices; localStorage is just the instant-paint cache. Backend = `migrate_06_dashboard_settings.sql` (additive: seeds + extends the `admin_set_setting` whitelist to `snap_autoplay`/`stars`/`star_cfg`, R3's `current_round` + the password row untouched; adds `get_app_settings()`). One shared `app_config.admin_password_hash` governs both boards. tag `dash-v0.38`. **v0.55 (2026-07-02) shipped the TEACHER PORTAL (phases 1-3, grill record `.planning/2026-07-02-teacher-portal-DECISIONS.md`):** Phase 1 = staff photo pipeline (Drive folder -> Supabase Storage bucket `teacher-photos`, public-read objects keyed by teacher uuid; `migrate_10_photos.sql` adds teachers.photo_url/md5/ref + the hand-maintainable `photo_alias` table + service_role REST grants; `db/photo_match.mjs` fuzzy-seeds the alias map (124/149 auto-matched, report `db/photo_match_report.md`), `db/photo_mirror.mjs` = initial import/heal; a NEW Apps Script "AIS Teacher Photo Mirror" under admin.user (repo `dashboard/apps-script-photos/`, scriptId `1oDPbKNUFg8JCwne9WPUydXYBcYXIFoH_QLhriJYpU2Qn4PI908cxapg3`) syncs nightly 03:00, md5-skips, deletions propagate; photos NEVER in the Pages repo; `get_raw_snapshot`/`shapeSnapshot`/`export_snapshot.mjs` all carry photo_url). Phase 2 = the portal board: 5th masthead pill `Teacher portal` (`#portal` route); browse = yearbook wall of ALL 149 teachers grouped Secondary/Primary/Kindy/Other (full 2:3 portrait tiles + name scrim + SEAS chip + initials fallback, search + section chips in ONE control row); page = the "Star file" (navy band, white-framed FULL portrait + 3 drifting AIS logo stars, huge italic serif name, overall SEAS chip best-evidence-wins) over house tiles: SEAS spectrum (their obs dots + avg flag via `specHTML`), 8-type coverage rail (SPEA lit), R3 evidence lines with locked-record links, Next Steps summary (draft shows with a Draft badge until approved, mirroring Observations visibility). Cross-links both ways with the Observations drill-down; FLIP name motion; self-contained per-teacher render (SSO-ready). Phase 3 = Department Level in the R3 Obs menu, built LIVE from `teachers.dept` (mark a dept in the Sheet -> appears; nothing hardcoded): Humanities = navy dept band (head badge, team portrait stack, observed count, avg word, 6-point spread bars) + a teacher x 8-type matrix (tinted SEAS word chips, quiet dots for unassessed types, top-biased avatar crops so foreheads survive); row click FLIPs into that teacher's portal page; empty scopes = calm "landing with the new academic year". GOTCHA fixed twice: 400x600 staff portraits in squarer `object-fit:cover` boxes crop foreheads; every avatar now uses full 2:3 frames or `object-position:50% 12%%`. The masthead rail overflow-scrolls on phones (5 pills). Login-less for the demo (the gate is the soft lock); @ais.ae SSO + per-role RLS, and a general multi-round selector for the other boards (Coverage/Observations still pool all rounds), are the next steps. **v0.39-v0.40 (2026-06-25) = Phase D (the Report).** v0.39: the Snapshot gains a **Report** menu (one button -> **Simple** | **In Depth**); each report has a **Make Google Doc** export built by a NEW Apps Script web app `dashboard/apps-script-export/` (runs as admin.user via clasp; Docs Advanced Service uses the `addDocumentTab` API to make a real 2-tab Doc = clean main tab + References tab, each claim's number links to the tab; built from the LIVE `get_narrative` payload, calm fallback if Supabase is down, no password gate by design). Also v0.39 unified the narrative source: `export_snapshot.mjs` now bakes the round's `get_narrative` into `data.js` as `window.__AIS_NARR_ITEMS` and `applyNarrItems()` builds NARR + REF_INDEX from it at load (the hand-typed `NARR` constant was deleted; Supabase is the single source). v0.40: **In Depth report = comprehensive evidence.** A multi-agent Workflow (one agent per claim, reads the deduped per-phase+section lesson evidence in `/tmp/indepth/`, matches BY MEANING, returns verbatim quotes; every quote code-verified as an exact substring, anti-fabrication) replaced the thin 2-4 reference sample with ALL supporting lessons: **~499 references, avg ~13/statement** (routines bullet 2 -> 17), each with a **"why this counts"** note. The In Depth view prints every claim with all its evidence inline; Simple keeps it behind the side-panel markers; both export to a Doc carrying the why-notes. Threshold rule (Igor): statements with <=5 refs kept only where small-by-design; the one genuinely thin Secondary develop bullet was replaced with "Tighten lesson pace and time management" (8 proofs). Storage: `narrative_ref.explanation` (migrate_07), round-scoped, comparable next round; tooling `~/AIS-Data-Dashboard/db/` `indepth_prep.mjs` / `indepth_verify.mjs` / `indepth_seed.mjs` / `indepth_replace_secb4.mjs`. Remaining Phase D (gated on Igor's approval, Focus OS): recommendations (Acceptable -> Good/Very Good, from a linked Google Doc) + a reusable next-round prompt. tag `dash-v0.40`. **v0.41 (2026-06-25) rebuilt the In Depth report to the readable Simple layout** (Igor rejected the v0.40 inline-evidence dump as unreadable): `renderInDepth` now delegates to `renderSimpleReport` with an "In Depth" kicker, so references stay behind the side-panel markers (the ~499 comprehensive refs + why-notes unchanged); the inline-dump code + its `.idclaim`/`.idrefs`/`.idevhead` CSS were removed. For now In Depth reads ~identical to Simple; it diverges later when the Acceptable->Good recommendations (task 6) + a Summary (task 7) are added into it. Snapshot report only; Story/Detail/Coverage/Observations untouched; tag `dash-v0.41`. **v0.42 (2026-06-25) added the "How to move Acceptable to Good" coaching section to In Depth** (task 6; In Depth ONLY): per phase (PK + Secondary) a governor summary + deep "Where Coaching Could Help" directions, each grounded in the AIS progress rubric (Igor's coaching Doc `1j-W83H...`) + this round's evidence (what Good+ lessons did that Acceptable were flagged for missing) with a concrete move + a "what good looks like" line, all supporting lessons behind the marker (6-17 each, no teacher names). Stored round-scoped in Supabase as `narrative` section='coaching' (kind summary + recommendation) + `narrative_ref`, NO DDL/RPC change (generic get_narrative carries them), baked into data.js; front-end builds `RECS` in applyNarrItems + renders `recsSection()` only when kind==='indepth'. Decisions (Igor): dedicated section, per-phase (not per-subject), BOTH voices (governor + teacher), In Depth only, label "Where Coaching Could Help", deeper-per-point. Tooling `~/AIS-Data-Dashboard/db/recs_prep.mjs`+`recs_seed.mjs`. The Google Doc export does NOT yet carry the coaching section (next). Snapshot In Depth only; Coverage/Observations/Story/Detail untouched; tag `dash-v0.42`. **v0.43 (2026-06-25)** gave the two coaching governor summaries (`pk_coach_sum`/`secondary_coach_sum`) their own evidence marker (they were the only unreferenced claims in the section): `narrative_ref` rows = a balanced round-robin across each phase's directions (deduped to lessons, cap 12); front-end stores `summarySlug` in `buildRecsFromItems` + renders `refMark` on `.rec-gov`. tag `dash-v0.43`. **v0.44 (2026-06-25)** added the In Depth **executive summary** ("In summary", task 7): a lead (round picture + distribution shape, no marker like the scope line) + 4 referenced synthesis points (most consistent strength, main focus, PK-vs-Sec contrast, move-to-Good by phase). Stored round-scoped as `narrative` section='summary' phase='all' (lead kind='summary' + points kind='bullet') + `narrative_ref`; front-end builds `SUMMARY` in applyNarrItems + renders `execSection()` at the TOP of In Depth (after the method box). Seeded by `~/AIS-Data-Dashboard/db/recs_seed.mjs` (now seeds coaching + summary). In Depth only; tag `dash-v0.44`. NEXT: Google Doc export of the coaching + summary sections (deferred per Igor till after the summary), then task 8 (reusable next-round prompt). **v0.45 (2026-06-25)** made BOTH report Google Doc exports styled (the Doc-export ask): a navy COVER image (`dashboard/assets/report-cover.png`, a headless-Chrome render of the Story cover slide, inserted by URL via the Docs API so no new Apps Script scope) on page 1 of both; the Simple Doc = card-styled (white page, each section a 1-cell-table card, mirrors on-screen); the In Depth Doc = formal report (cover + Contents list + In summary + phases + compare + How-to-move-Acceptable-to-Good). `buildDocModel(items,kind)` now sends kind + cover + summary + coaching; `dashboard/apps-script-export/Code.gs` rewritten into `buildSimple` (cards) + `buildInDepth` (report) sharing the marker + References-tab machinery (refs tab + cross-tab links kept). Docs limits: square-cornered cards, Contents is a styled list not an auto page-numbered TOC. clasp redeployed in place (same /exec). Export-only; on-screen dashboard untouched; tag `dash-v0.45`. Cover generator: `/tmp/covergen/cover.html` + Chrome --headless --screenshot. **Doc design then reworked to the approved AIS-branded v3 concept** (3 page mockups rendered via headless Chrome -> Igor picked v3): Playfair Display headings + Lato body, AIS navy + gold accent (green/terracotta only on the section labels), one consistent bullet style, a stat callout + insight cards (1-cell tables), first tab renamed 'Report' (`updateDocumentTabProperties`); `Code.gs` rebuilt into `buildSimple` (cards) + `buildInDepth` (formal report + Contents) with a shared styled-paragraph helper; clasp redeployed @8. Verified by exporting each Doc to PDF (`gws drive files export`) and reading it. Mockups + cover gen in `/tmp/covergen/`. **v0.46 (2026-06-26)** grounded the coaching section in the AIS progress rubric's TWO aspects (Igor: it must REFER to the rubric, the school's progression standard): a `coach_intro` row frames aspect A "Progress in Lessons" (the Acceptable->Good outcome) + links the rubric; each direction shows aspect B "Facilitating better than expected progress" (the teaching descriptor, stored in `narrative.method`) under the move + its R3 evidence. Seeded by `recs_seed.mjs` (RUBRIC map + COACH_INTRO + method column); front-end builds `RECS.intro` + renders `.rec-rubric` per direction; `Code.gs` renders the intro + link + per-point rubric line in the In Depth Doc. RUBRIC_URL = the Doc 1j-W83. On-screen + Doc; tag `dash-v0.46`. **v0.47 (2026-06-26) tidied the Report/Snapshot buttons + neutralised the report colours + restyled the Settings gate** (Igor, Focus OS `da8b8a2a` + the gate task): opening a report is now a standalone route (`#report` / `#report-indepth`, bookmarkable, browser-back returns to the live Snapshot via `openReport`/`exitReport` + the hashchange handler); on the report page the Story/Detail toggle + the Report dropdown are replaced by a **Back** button and a solid **Simple | In Depth** switch (`#reportTabs`), and `#viewTabs`/`#reportWrap` hide while `#reportTabs` shows. The **report body is now monochrome navy** (the green `--g` / terracotta `--r5` / gold `--y` accents on `.rep-gh`/`.rep-insh`/`.rin`, the scope avg word, `.rec-look`/`.rec-rubric b` and the `.rep .big em` R3 title are all `var(--ink)`); the live Snapshot header + Story + Detail keep their colour. The Snapshot tab-row controls (phase tabs / Story-Detail / Present / Report / report tabs) are unified to one 38px size (the round step + A-/A+ icon buttons left alone). The **admin Settings gate `#setGate` is restyled to mirror the R3 form's admin card** (gradient top-bar, AIS·Admin eyebrow, Settings display heading, framed input, ghost Close `#setGateClose` + green Unlock; the corner × removed), theme-token based so light + dark both hold. Snapshot-only; Coverage + Observations + the Doc export untouched; verified in-browser both themes, zero console errors. tag `dash-v0.47`. The **Google Doc export (`Code.gs`) was then flattened to match** (Igor: dashboard left as-is, only the Doc): all cream `card()` fills (insight/scope/method/phase/compare boxes) removed -> flat text, and the gold/green/terracotta header labels (kicker, INSIGHTS, What-is-working/Where-to-focus, WHERE COACHING COULD HELP, AIS-rubric-Facilitating, contents/section numbers, the accent bar) all taken to navy/grey, so the exported Doc reads as a clean monochrome governance report (blue `[n]` ref links + cover image kept); clasp push + redeploy in place @10, verified by exporting the In Depth Doc to PDF. **v0.48 (2026-06-27) made the Snapshot tab-row buttons truly uniform** (Igor: the v0.47 "38px" pass equalised the outer height but they still LOOKED different sizes, root cause found via computed styles): the active phase + Story/Detail pills were only 30px inside a faint 0.66 frame and the Report button was a faded 0.66 fill, so next to Present's solid 38px 0.86 pill they read as different sizes. Fix: the phase/view/report-tab CONTAINERS lose their frame (transparent, no padding) and every active/action control (`.ptab.on`/`.vtab.on`/`.rtab.on`/`.repbtn`/`.sbtn.wide` Present/`.repback` Back) is the SAME solid 38px pill (`--panel-2` 0.86 + `--edge-soft` + shadow + `--ink`); unselected tabs are transparent ghost text at full 38px height. Dashboard report styling left untouched per Igor (it looks good). tag `dash-v0.48`. **v0.49 (2026-06-27) shipped the per-teacher coaching engine** (Focus OS `e5ff8b32`, grilled to spec; the per-teacher analogue of the phase-level coaching, and part of the task-8 reusable prompt). Every June-observed teacher (~104) gets a personal **"Next Steps & Improvement"** note in the Observations drill-down (`#nextSteps`), generated from their OWN pooled R3 evidence (strengths/weakness/observer_notes across all their lessons, best rating winning) + the AIS rubric, coaching BOTH aspects: **Progress in Lessons** (r3_02, 6-pt) and **Facilitating** (the moves ladder, 5-pt; Teaching r3_05 maps Weak/VeryWeak->Beginner, Acceptable->Emerging, Good->Good, VeryGood->Great, Outstanding->Outstanding), each **current -> next level FLOORED AT GOOD** (Outstanding -> sustain & share). Output = a standing line + 3-5 sentence summary + 2-4 bullets (each = a next-band Facilitating move + a **verbatim** line from their own lessons + a Look-for + 1-2 **anonymous same-subject exemplars** from other lessons this round, linking the locked R3 record). **AI-drafted (multi-agent Workflow over the 104, model sonnet) then anti-fab verified** (every evidence + exemplar quote is an exact substring of the source; 2 paraphrased bullets dropped, 0 fabricated exemplars) then coach-approved. **Visible to everyone with the dashboard password** (Igor 2026-06-27: NO admin gate on viewing now, SSO roles restrict later); **edit + approve are admin-gated** (`verify_admin_password` / `admin_set_teacher_next_step`, the in-place `requireAdmin` gate, not a navigation); each note **exports to a personal Google Doc** (`Code.gs` `buildCoaching`, single-tab, manual share). Backend: round-scoped `teacher_next_steps` + `coaching_rubric` tables, anon `get_teacher_next_steps`/`get_coaching_rubric` reads + admin-gated write (`migrate_08`); baked into the gitignored `data.js` as `window.__AIS_NEXTSTEPS`. Tooling: `~/AIS-Data-Dashboard/db/` `coach_prep.mjs` (per-teacher inputs -> /tmp/coach/in), `coach_verify.mjs` (anti-fab -> /tmp/coach/clean), `coach_seed.mjs` (-> Supabase, re-runnable, keeps approved edits). Observations drill-down + the Doc; Snapshot/Coverage/report untouched. Verified live (104 notes via the anon RPC, approve/revert via the admin RPC, the coaching Doc exported to PDF). tag `dash-v0.49`. **v0.50 (2026-06-28) reworked the per-teacher coaching note** (presentation + grounding; Igor's post-v0.49 review): Facilitating now sits on the SAME 6-pt scale as Progress (from r3_05, floored at Good -> reads "Good -> Very Good"; the 5-pt Beginner/Emerging/Great band NAMES are internal-only as the move-descriptor source, never in prose); each move's rubric grounding is reworded to a plain-English linked line ("In the AIS OTP Progress Rubric, to facilitate better than expected progress at a [band] level, the teacher [descriptor]") + a rubric link; the in-house "what this looks like elsewhere" exemplars + their other-teacher record links (an anonymity breach) are BINNED and replaced by 18 verified ONLINE techniques (6 themes x 3, in `~/AIS-Data-Dashboard/coach_online_sources.md`: Edutopia, Structural Learning, Chartered College, Third Space, InnerDrive, Tips for Teachers) WRITTEN OUT in words + a source link per move; layout = **two-column focus cards** (action left: move/look-for/technique/source; grounding right: rubric line + verbatim evidence) with a current->target SEAS ladder per aspect + a colour-coded theme chip (chosen by Igor from a 3-way preview, shown inline via the visualize artifact tool). All 104 notes regenerated (multi-agent Workflow, model sonnet) + re-verified: `coach_verify` now also checks every rubric line is verbatim rubric text AND every source URL is one of the 18. `coach_prep.mjs` attaches the online sources + the 6-pt facilitating display + the 5-pt internal move_descriptor (sustain teachers ground on the Outstanding descriptor). `Code.gs buildCoaching` updated to match (technique + source link, no record links). Observations drill-down + the Doc; Snapshot/Coverage/report untouched. tag `dash-v0.50`. **v0.51 (2026-06-28)** polished the coaching note (Igor's v0.50 review, front-end only, no regen): the observation session card shows its **round** (e.g. "R3 June 26") beside the Inspection details heading (`o.round`, already in the data); the note **summary fills full card width** (62ch cap removed); **"Facilitating" -> "Facilitating better progress"** (ladder label + Doc standing line); the right column (rubric grounding + lesson quote, `.ns-rub`/`.ns-q`) is **bigger + darker** and the left **technique matches the summary size** (`.ns-tech` 14px); and the **edit pane drops the "Move N" jargon** -> each focus area headed by its THEME (Student dialogue/Questioning/...), the rubric line shown read-only above, and editable Recommended action / Look for / Technique boxes (rubric text + verbatim quote stay locked). tag `dash-v0.51`. DEFERRED: online exemplar sources; @ais.ae SSO + per-role RLS + per-coach delegation + auto-share + in-app teacher view (task `243b0534`); voice tuning after Igor reads the batch. (The per-teacher application of the rubric = the new big Focus OS task, per-teacher Next Steps.) **v0.52 (2026-07-01)** moved the observation session card's round tag (`.oround`, e.g. "R3 June 26") from the far right of the Inspection details header to sit beside the heading and its arrow on the LEFT, on the same line (dropped `margin-left:auto`; the header's flex `gap` places it 6px after the chevron). Session-card-only (Observations drill-down; renders everywhere the card appears); Snapshot / Coverage / coaching note / report untouched. tag `dash-v0.52`. **v0.53 (2026-07-01)** added a **round selector** to the Snapshot (February | June | Compare; a top-level `snapRound` axis, plus an All / PK / Secondary `cmpPhase` sub-filter for February + Compare). **February** renders a numbers-only board (lessons, teachers, avg-progress gauge, 6-pt distribution) since only progress ratings + short notes were recorded that round (no strengths/areas). **Compare** puts February against June: delta tiles (direction-aware, a lower rating = better), a paired distribution (reusing `renderDiv`), a **movement slopegraph** + a **February to June transition matrix** for the teachers seen in BOTH rounds, and a net improved/held/declined summary. Movement uses the shared metric (progress `r3_02`); the matched cohort is kept separate from per-round aggregates; `snapAgg` gained a round param (June callers default unchanged, verified regression-clean). Snapshot-only, isolated (reads `D`, never mutates); June Story/Detail/Report + Coverage + Observations untouched. tag `dash-v0.53`. **v0.54 (2026-07-02)** collapsed the round controls to ONE row + ONE Compare entry (Igor: no two rows of buttons): the v0.53 round row is gone; the Compare button beside the phase tabs opens a menu with two generated sections, Compare (PK vs Secondary within June; February vs June teacher movement) and View a round (February numbers board; June default), all round entries auto-generated from `D.rounds` (`buildCmpMenu`/`roundsChrono`/`roundLabel`; `cmpRounds`/`viewRound` state; the All/PK/Sec filter renders INSIDE the Feb + Compare boards via `bFilter`). Plus the **Better Never Stops entrance**: the three AIS logo stars (Approved Colours sheet "on Star" hexes, Red #EF343A / Blue #1257FF / Yellow #EDFF21) comet in left/top/bottom on GSAP MotionPathPlugin paths carrying the motto, hold 2s, then drift into the star-field as the title card assembles (`entryStars()` inside `runEntry`, 5.5s + beat, click-skip kept; dark theme adds a congruent light stroke + glow on all three; no gsap / no plugin / reduced-motion falls back to the plain card with the original 2.6s hold, hard rule 4 intact). tag `dash-v0.54`. **v0.56-v0.59 (2026-07-03/04) turned the Teacher portal page into the teacher hub** (Focus OS `9b53c0f4` + Igor's live iteration): v0.56 moved the 8-type coverage rail INTO the navy hero as the portal menu and made the SPEA section approval-gated, REUSING the Observations components via a `teacherView` flag on `renderObsList`/`renderNextSteps` (teacher-side copies carry NO verbatim observer notes, NO locked-record links, NO admin buttons; the Observations board keeps everything) + the staff photo on the Observations header card; v0.57 cleaned the hero (real judgement abbreviations, "Open in Observations" removed, teachers never reach admin boards; search decoy prefix removed; intro star mustard #FFBA14); v0.58 made coaching notes PER-ROUND (`teacher_next_steps` keyed teacher+round, per-round Edit/Approve/Make-Doc, 121 neutral February drafts seeded) and the portal a ROUND STACK (one card per APPROVED round, round-scoped pop-downs); v0.59 rebuilt the hero as the NUMBERED SECTION LIST in Igor's locked order (Probationary / OTP & APR / Lesson Obs (APR) / SPEA R3 / ESCM / Walkthroughs / Links+AI Chat coming soon), per-section judgement scales, only SPEA live, coverage rail retired. RULE: the portal hero is the TEACHER'S hub, never an R3 artefact; no admin controls or R3 branding in it. (v0.60 SHIPPED 2026-07-05: stripped hero, red New Notification badge + Supabase `portal_seen` seen-state via migrate_11, unified serif board headers, and the portal round summary now fills the full card width.)
- Architecture (data, auth, schema, boards, attendance): `.planning/2026-06-12-dashboard-architecture-DECISIONS.md`
- Design + motion (Cinematic, LIGHT default, AIS star-field, 6-point SEAS scale, Spectrum/Arc/matrix): `.planning/2026-06-15-dashboard-design-DECISIONS.md`
- Living spec, open in a browser: `dashboard-previews/design-spec.html`
- Current DB + decision state: memory `project_dashboard_architecture.md`. Supabase ref `rfbetrcevtmisknndpgg`; tooling + creds at `~/AIS-Data-Dashboard/` (never echo secrets).
- Next-round regeneration: the reusable prompts that recreate every AI-written text surface (narrative/Story/Detail, In-Depth refs, Acceptable->Good recommendations, per-teacher coaching) for a new R3 round are stored 3 ways, Supabase `ai_prompts` via `get_ai_prompts()`, repo `.planning/2026-06-28-next-round-rebuild-prompts.md` (+ `.json`), and Focus OS task `c1ace004`; canonical source `~/AIS-Data-Dashboard/db/ai_prompts_build.mjs` (re-run to sync all three). Verifying them + a test sandbox = Focus OS `ea033575`.
- Teacher portal (next build, grilled + LOCKED 2026-07-02): decision record `.planning/2026-07-02-teacher-portal-DECISIONS.md`; Focus OS urgent (1) 8f924255 photo mirror -> (2) 2aa570d2 portal page -> (3) b6738997 dept view; staff photos = Drive folder (verified) mirrored to Supabase Storage via Apps Script daily trigger, NEVER in the public Pages repo.
- Latest status: newest doc in `handoff/`.

**Dashboard build chain + hard rules (see also memory):**
- Master = `dashboard/src/index.html` (edit this). It reads `window.__AIS_DATA`; **v0.21 wired the live-Supabase swap**: `loadLive()` fetches the `get_raw_snapshot` RPC, runs `shapeSnapshot()` (a verbatim port of `export_snapshot.mjs`), and replaces `D` on each load, falling back to the baked `data.js` if Supabase is unreachable.
- Data = **LIVE Supabase on each load** via RPC `public.get_raw_snapshot()` (SECURITY DEFINER, EXECUTE to anon; SQL `~/AIS-Data-Dashboard/db/migrate_03_dashboard_rpc.sql`, apply with `node db/apply.mjs <file>`). The baked `data.js` is the **first-paint + offline fallback** (regenerate: `node ~/AIS-Data-Dashboard/db/export_snapshot.mjs "<repo>/dashboard/data.js"`). **`dashboard/data.js` is GITIGNORED on purpose**, it holds real teacher names + ratings in cleartext and must never reach Pages. The Supabase URL + publishable key are inlined in the master and ship inside the StatiCrypt-encrypted page.
- Deploy = `./dashboard/encrypt.sh` (inlines data.js ENCRYPTED into `dashboard/index.html` via StatiCrypt, password `ais2026ais`). Never hand-edit `dashboard/index.html`; it is generated. Then commit + push main; the self-healing Pages workflow (`.github/workflows/pages.yml`) auto-builds, auto-retries the publish and deploys, no manual rebuild.
- **Versioning (do not skip, the footer was stuck at v0.1 through v0.6):** every dashboard release bumps the footer badge in `dashboard/src/index.html`, adds a row to the Settings "Build history" panel, appends an entry to `dashboard/CHANGELOG.md`, and is git-tagged `dash-vX.Y` (rollback safety).
- All dashboard motion is enhancement only (final state applies without requestAnimationFrame): preserve this so a frozen ticker (hidden/bfcached tab, which StatiCrypt's document.write makes likely) never strands the title card or content.
- The gate is SOFT: the password is publicly fetchable via served files (CLAUDE.md, both encrypt.sh). Treat the URL as soft-gated for a controlled demo, not secure; real access control is the planned SSO + RLS.

---

## File map

```
Data-Representation/                          github.com/Rogerceaser21/Data-Representation (PRIVATE)
├── index.html                                5-slide pitch deck; P3 links to both forms
├── lesson-observation-form.html              3-line redirect stub (preserves old URL after the v0.10 move)
├── apps-script/                              Lesson Observation backend (clasp, admin.user)
│   ├── .clasp.json                           scriptId 1wfwv-3le3lMFeiV6E_hghRQ3F2kAiInNaNlnQbipx4bLMFH9N0yRKXEL
│   ├── 00_Config.gs                          SHEET_ID + getColumns(), 80+ col schema, DO NOT REORDER
│   ├── 01_doPost.gs                          append row, generates AIS-OBS-YYYYMMDD-HHMM
│   ├── 02_doGet.gs                           ?id=AIS-OBS-... returns record as JSON
│   ├── 03_helpers.gs                         jsonOut + forceAuth (one-shot OAuth trigger; keep)
│   └── appsscript.json                       webapp: ANYONE_ANONYMOUS, USER_DEPLOYING
├── Assets/
│   ├── brand/AIS Logo/                       AIS White.png + AIS Navy.png (only runtime brand assets)
│   ├── brand/email-signature-primary.png     Igor's primary-school email signature banner (Pages-hosted for the R3 inspector email)
│   ├── Lesson Observation Form/
│   │   └── lesson-observation-form.html      THE Lesson Observation form (moved here in v0.10)
│   └── R3/
│       ├── r3-evidence-form.html             ENCRYPTED output (StatiCrypt) · the inspectors' form GitHub Pages serves
│       ├── r3-record.html                    UNGATED teacher viewer (v0.42) · generated by encrypt.sh from the master · record-link-only via window.R3_VIEWER
│       ├── src/r3-evidence-form.html         MASTER source · edit this · v0.34 opaque-keyed Tom Selects, v0.37 resilient options fetch, v0.50 Evidence Pad
│       ├── password-template.html            AIS-themed StatiCrypt gate (dark / light, AIS yellow, gate prewarm)
│       ├── encrypt.sh                        build script · runs StatiCrypt with password "ais2026ais"
│       ├── .staticrypt.json                  encryption salt (safe to commit)
│       ├── lib/                              vendored Tom Select + Konva 9.3.22 + perfect-freehand 1.2.3 (Evidence Pad, v0.50; load after decrypt)
│       ├── Google file links.md              quick-access Sheet + Apps Script + Gmail links
│       ├── README.md                         R3-specific deploy workflow + v0.30-v0.34 changelog
│       ├── R3 Evidence Form Template.docx    source template from SPEA
│       ├── apps-script/                      R3 backend (clasp, admin.user)
│       │   ├── .clasp.json                   scriptId 1BYxWpSsqs-48AKnWH4BUQonAKHqDcY6VbiWA1SNbhBZT0QgDzLUatgRn
│       │   ├── 00_Config.gs                  getSheetId() via PropertiesService + getR3Columns() 49-col schema + BACKUP_EMAIL_TO + FORM_PUBLIC_URL + SUPABASE_URL + getSupabaseSecret() (v0.47) + getAnthropicKey() (v0.50)
│       │   ├── 01_doPost.gs                  append row (header self-heal for appended cols), generate record_token, auto-compute duration (v0.32), email-on-submit + CC inspector (v0.31) + pad attachments (v0.50), Supabase dual-write (v0.47), routes action=extract_pad (v0.50)
│       │   ├── 02_doGet.gs                   ?id=AIS-R3-...&token=... · safeForSelector + CacheService 5-min TTL + clearOptionsCache (v0.31, v0.33)
│       │   ├── 03_helpers.gs                 jsonOut, forceAuth, bootstrap (creates Sheet + tabs + seeds + stores SHEET_ID)
│       │   ├── 04_TeacherLoader.gs           pulls staff from 6 Workspace Groups via Admin SDK (recurses nested groups)
│       │   ├── 05_Supabase.gs                v0.47 dual-write · pushToSupabase() POSTs each submission to the ingest_r3 RPC (service_role key from Script Properties)
│       │   ├── 06_PadExtract.gs              v0.50 Evidence Pad · handlePadExtract() = Claude vision (claude-sonnet-5, JSON schema) per pad page + fetchPadImages() for the email attachments
│       │   └── appsscript.json               webapp + AdminDirectory advanced service + admin.directory.* + script.send_mail + script.external_request scopes
│       └── 2026-05-28_email_R3-Data...md     correspondence record (R3 kickoff email to Dave, CC Leon + Steve)
├── SPEA Data Report/                         worked-example source docs (Jo Mare Kruger portrait, OTP, probation form)
├── .planning/
│   ├── v0.10-r3-form-checklist.md            multi-phase plan + acceptance criteria + IDs
│   ├── 2026-06-11-moderation-and-dashboard-plan.md   Jobs 0-3 (Job 0 + Job 1 done)
│   └── 2026-06-12-dashboard-architecture-DECISIONS.md  AUTHORITATIVE grill record (Q1-Q11) for the Supabase dashboard
├── dashboard/                                 LIVE gated dashboard, Snapshot landing (tag dash-v0.60)
│   └── apps-script-export/                     Apps Script web app (admin.user) that builds the Report Google Docs (Phase D)
│   ├── index.html                             ENCRYPTED deploy artifact (StatiCrypt, data inlined) · generated, never hand-edit
│   ├── src/index.html                         MASTER · edit this · Snapshot (Story + Detail) + Coverage + Observations + Settings
│   ├── data.js                                GITIGNORED baked Supabase snapshot (window.__AIS_DATA) · real names+ratings, never to Pages
│   ├── encrypt.sh                             build · inlines data.js ENCRYPTED into index.html (password ais2026ais)
│   ├── password-template.html                 AIS-themed StatiCrypt gate (re-themed from R3)
│   ├── CHANGELOG.md                           per-release changelog (every dash-vX.Y)
│   ├── apps-script-photos/                    staff-photo Drive->Supabase mirror (clasp, admin.user, daily 03:00 trigger; README has setup)
│   └── assets/                                ais-white.png + ais-navy.png + snapshot-cover.jpg + story/ illustrated motifs (public, non-sensitive)
├── dashboard-previews/                        dashboard design concepts · spea-dashboard.html = dashboard-v1 (parked); built from index.html slides 3-4
├── README.md                                 STALE (still describes v0.1 pitch only)
├── CLAUDE.md                                 this file
└── .gitignore
```

Each form is self-contained at runtime. All CSS and JS are inline; only Google Fonts and the AIS logo PNGs are external. Asset paths inside the form HTML use `../brand/AIS Logo/...` since both forms live one level under `Assets/`.

---

## How the pieces connect

Two parallel pipelines, both running under admin.user@ais.ae:

```
                  admin.user@ais.ae Drive
                  ┌──────────────────┬──────────────────┐
                  │ AIS Lesson Obs   │ AIS R3 Evidence  │
                  │ Submissions tab  │ Submissions + 4  │
                  │ (80+ cols)       │ reference tabs   │
                  └────────▲─────────┴─────────▲────────┘
                           │                   │
                POST + GET │                   │ POST + GET + options
                           │                   │
        ┌──────────────────┴───┐   ┌───────────┴──────────┐
        │ Apps Script "AIS     │   │ Apps Script "AIS R3  │
        │ Lesson Observation"  │   │ Evidence Web App"    │
        │ scriptId 1wfwv-3le.. │   │ scriptId 1BYxWpSsqs..│
        └──────────▲───────────┘   └───────────▲──────────┘
                   │                            │
    ┌──────────────┴────────────┐   ┌──────────┴──────────────┐
    │ lesson-observation-       │   │ r3-evidence-form.html   │
    │ form.html                 │   │ (in Assets/R3/)         │
    │ (in Assets/Lesson         │   │                         │
    │  Observation Form/)       │   │                         │
    └──────────────┬────────────┘   └──────────┬──────────────┘
                   │                            │
                   └──────────┬─────────────────┘
                              │
                        index.html (deck)
                  P3 has buttons linking to both
```

---

## Hard rules · do not break

1. **Sheet column order is load-bearing for both Sheets.** `apps-script/00_Config.gs` `getColumns()` (Lesson Obs, 80+ cols) and `Assets/R3/apps-script/00_Config.gs` `getR3Columns()` (R3, 49 cols since v0.50: `record_token` then the appended `evidence_pad_id`) define schemas auto-created on first POST. Existing rows are positional. *Append* new columns at the end if needed; `01_doPost.gs` self-heals the header row for appended columns. Never reorder or insert in the middle. `record_token` remains the record-lookup key regardless of position (rule 10).
2. **Record ID formats are `AIS-OBS-YYYYMMDD-HHMM` and (R3, v0.46+) `AIS-R3-YYYYMMDD-HHMMSS`.** Generated server-side. The R3 id is now a **display label only** (v0.46): it is minute-resolution pre-v0.46 and COLLIDED for same-minute submissions, so it is NOT a unique key. R3 record lookup keys on the unique `record_token` instead (see rule 10), so the colliding ids are harmless. v0.46 added seconds to NEW R3 ids to keep labels unique. The Lesson Obs id is still the lookup key for that form, so don't change its format. Don't regenerate existing ids.
3. **`WEB_APP_URL` constants must match the deployed `/exec` URL for each form.** If you `clasp deploy` a new version that gets a new `/exec` URL, update the matching constant in the form HTML and ship.
   - Lesson Observation: `Assets/Lesson Observation Form/lesson-observation-form.html`, search for `const WEB_APP_URL`
   - R3 master: `Assets/R3/src/r3-evidence-form.html` (the encrypted output at `Assets/R3/r3-evidence-form.html` is regenerated from this)
4. **Repo stays private** until ratings are real OR the form is StatiCrypt-encrypted (R3 form is, from v0.29). Real teacher names sit next to fabricated ratings.
5. **Don't commit the `.docx` file's noise diffs.** `SPEA Data Report/Lesson Observation - Igor- 12_11.docx` flutters by a byte every time Word touches it. Skip unless content actually changed.
6. **Every push bumps the visible version label AND tags.** Bump version in: cover term-tag, every masthead `.school` text, P5 floor, version-badge element, AND both form footers (R3 footer lives in the **master** at `Assets/R3/src/r3-evidence-form.html`). Commit msg starts `vX.Y · summary`. `git tag vX.Y` after the commit. `git push origin main --tags`.
7. **R3 reference tab schemas are load-bearing.** The R3 form reads Inspectors / Curriculum / Subjects / Teachers from named tabs via `?action=options`. Single-column tabs assume header row + data rows; Teachers tab uses column A = name only (v0.28). The form's `setupSearchableTeacher` + `loadDropdownOptions` break silently if the tab schemas shift.
8. **The redirect stub at `lesson-observation-form.html` (root) must be preserved.** It catches old URLs (`/lesson-observation-form.html?id=AIS-OBS-...`) and forwards to the new path. Removing it 404s any reference shared before the v0.10 move.
9. **R3 master / encrypted split (v0.29), plus the teacher viewer (v0.42).** Edit `Assets/R3/src/r3-evidence-form.html`. Run `Assets/R3/encrypt.sh` from anywhere; it regenerates BOTH outputs: `Assets/R3/r3-evidence-form.html` (the StatiCrypt-encrypted inspectors' form) and `Assets/R3/r3-record.html` (the ungated teacher viewer, same master with `window.R3_VIEWER=true` injected; record-link-only, shows a calm landing card without `?id`). Never hand-edit either output. School-wide gate password is `ais2026ais` (hardcoded in `encrypt.sh`; rotate by editing + re-encrypting). Teachers do NOT get the gate password; their access is their personal `r3-record.html?id=...&token=...` link.
10. **R3 locked-record viewing keys on the token (v0.46), which is also required.** The URL is `?id=AIS-R3-...&token=<32-hex>` (legacy) or `?token=<32-hex>` (token-only, also accepted). `02_doGet.gs getRecordById` finds the row by **`record_token`** (two UUIDs, unique per submission, found by header name so the column's position is irrelevant), NOT by `record_id` (which collides, minute-resolution). The id is ignored for matching; it's a display label. The token is generated in `01_doPost.gs` per submission and included in the backup email to `admin.user@ais.ae`. An exact token match is required; any non-match (wrong token, no token) returns a generic "Record not found" (so attackers can't probe). Pre-v0.29 test records have no token and are not viewable. **Don't revert the lookup to id-first: it reintroduces the same-minute collision bug (17 ids / 37 records during live inspection week were unreachable).**
11. **Never put user-data strings into Tom Select option `value` (v0.34 iOS WebKit fix).** Tom Select v2.6.1's internal `addSlashes` only escapes `\ " '` and does NOT escape NBSP, ZWSP, U+2028/9, control chars, or other CSS-meaningful chars. iOS WebKit's strict selector parser throws `SyntaxError: The string did not match the expected pattern` from the resulting malformed selectors, blanking the dropdowns. StatiCrypt's `document.write` aggravates this by triggering stricter post-load parsing. **Rule:** teacher / inspector / subject (any Tom Select fed user-controlled strings) must use **opaque indices** as `value` (e.g. `t0`, `i0`, `s0`...) and put the human-readable name in `text`. Translate `value` back to `text` via `tomSelects[name].options[value].text` at submit + save time so the Sheet still stores names. See `src/r3-evidence-form.html` `populateTomSelect`, `refreshSubjectOptions`, `submitForm`, `saveForm`, `loadClosedRecord` for the pattern.
12. **Stakeholders must never see an error or a stuck spinner (v0.37).** The R3 form is used by the school's most senior staff; Igor's absolute rule is "no error UI, ever." The dropdown options fetch in `src/r3-evidence-form.html` `loadDropdownOptions()` therefore: caps each attempt at 7s via `AbortController`+`setTimeout` (NOT `AbortSignal.timeout`, which is iOS 16+ only), silently retries behind the existing frosted overlay (no toast, no error text), reloads once as a last resort (guarded by a `?_r=1` flag against loops), and reloads on `pageshow` when `event.persisted` is true (a stale iOS tab restored from bfcache, the documented cause of the "stuck on loading" hang). When changing this code path, preserve all four behaviours: timeout, silent retry, bfcache reload, guarded last-resort reload. Never surface a visible failure state to the user.
13. **Record views never write the localStorage draft (v0.44).** `loadClosedRecord` fires `change` events while populating fields; the debounced autosave must not capture them, or the viewed record silently overwrites the shared draft (both the viewer and the gated form share the GitHub Pages origin). `saveForm` bails when `CLOSED_RECORD_VIEW` is true (viewer build via `window.R3_VIEWER`, or `?id` in the URL); the draft key is `ais-r3-form-v2` (v1 was polluted and is deleted on load). Preserve this guard when touching autosave or the closed-record path. **v0.45 closed the reverse leak (draft -> record view):** the draft was bleeding its Teacher/Inspector over a loaded record (a viewed record showed "Igor Sesar / Saliq Jaral"), and the opaque Tom Selects rendered blank when the staff options landed after the record fetch. Three rules now hold, keep them: (a) `readRestoredFromLocalStorage` returns `''` whenever `CLOSED_RECORD_VIEW` (no draft ever feeds a record view); (b) `populateTomSelect` resolves Teacher/Inspector from `dataset.pendingValue` (the record's text, set by `loadClosedRecord`) BEFORE the draft, mirroring `refreshSubjectOptions`, so they populate regardless of fetch order; (c) `loadForm` is gated on `!CLOSED_RECORD_VIEW` (not just `!R3_VIEWER`). Also `loadClosedRecord` is now resilient (9s `AbortController` timeout + silent retry, only a real server `success:false` surfaces) like `loadDropdownOptions`.
14. **R3 -> Supabase dual-write contract (v0.47). The Google Sheet is the source of truth; Supabase is the mirror.** `01_doPost.gs` appends the Sheet row FIRST, then calls `05_Supabase.gs pushToSupabase()` inside its own try/catch. A Supabase failure is logged and swallowed (never surfaced; hard rule 12) so no data is ever lost and the inspector never sees an error. The bridge POSTs the full row (same column mapping as the Sheet) to the `ingest_r3(payload jsonb)` Supabase RPC, which is **idempotent on `record_token`** (`assessments.source_ref`), so retries/re-submits never duplicate. **Auth:** `ingest_r3` is `SECURITY DEFINER`, EXECUTE granted to **service_role only** (anon is denied, verified 401); the service_role key lives in R3 **Script Properties** (`SUPABASE_SECRET_KEY`), never in code. **It MUST be the legacy service_role JWT (`eyJ...`, ~219 chars; env `SUPABASE_SERVICE_ROLE_KEY`), NOT the new `sb_secret_...` key** — the new secret keys reject any browser-like caller (`401 "Forbidden use of secret API key in browser"`) and Apps Script's `UrlFetchApp` sends a browser-ish User-Agent it cannot override, so `sb_secret_` keys can never work from Apps Script. The SQL is `~/AIS-Data-Dashboard/db/migrate_04_r3_ingest_and_config.sql` (applied via `node db/apply.mjs`); heal any drift with `db/backfill_r3.mjs`, check drift with `db/verify_mirror.mjs`. **Admin Settings cog (v0.47):** a discreet gear on the R3 form opens a password-gated panel (password `AvasIgor`, stored bcrypt-hashed in Supabase `app_config`, checked server-side by the `verify_admin_password` RPC, reusable by any component) that sets the current inspection Round via `admin_set_setting` (key-whitelisted to `current_round`). `ingest_r3` stamps each new record with `app_config.current_round`. The form's Supabase **publishable** key (anon, client-safe) is inlined in the master and ships INSIDE the StatiCrypt-encrypted form; `encrypt.sh` **blanks `SB_KEY`/`SB_URL` in the ungated viewer build** (`r3-record.html`) because the publishable key can call `get_raw_snapshot` (all teacher names + ratings), so it must never ship cleartext on the public viewer. Keep that strip when touching `encrypt.sh`.
15. **Evidence Pad contract (v0.50).** The R3 form's pad (photos + Apple Pencil ink) is Konva + perfect-freehand (vendored in `Assets/R3/lib/`, MIT). Its four moving parts: (a) **draft** lives in IndexedDB `ais-r3-pad-v1` on-device (photos as ArrayBuffers, never Blobs — WebKit IndexedDB Blob bugs), cleared on Reset/soft-reset, never written in `CLOSED_RECORD_VIEW`; (b) **storage**: on submit the flattened pages (JPEG ≤1280px) + `ink.json` vectors upload to the PRIVATE Supabase bucket `evidence-pads/<pad_id>/` under an **anon INSERT-only policy** (the publishable key can write but never read/overwrite/list — classroom photos can contain student faces, so no public/anon read path may ever be added; reads are service-key only: backup-email attachments now, an admin-gated dashboard viewer later), and `evidence_pad_id` is stamped on the record (49th Sheet column; rides into Supabase `assessments.content` automatically since `ingest_r3` stores the full payload); (c) **extraction**: `doPost` routes `{action:'extract_pad', image}` (one page per call, fanned out in parallel from the client — keeps each call far inside UrlFetchApp's ~60s outbound cap) to `06_PadExtract.gs`, which calls Claude `claude-sonnet-5` vision with a forced JSON schema and returns items for the four free-text targets; the form shows a review step and APPENDS on approval, never auto-inserts. The **Anthropic API key lives in Supabase `app_config` under `anthropic_api_key`** (RLS-locked; no anon RPC exposes it; `admin_set_setting`'s whitelist must never include it), seeded/rotated by `~/AIS-Data-Dashboard/db/seed_anthropic_key.mjs`, fetched by Apps Script with the service key + cached 1h — no Script Property needed. Note the rule-14 `sb_secret_` UA gotcha does NOT apply to `api.anthropic.com` (it authenticates on `x-api-key`, UA-agnostic); (d) **coexistence with iPadOS Scribble**: the form's textareas keep native Scribble handwriting-to-text; the pad canvas uses `touch-action:none` + `touchmove preventDefault` so Scribble can't steal strokes (community workaround, no official web opt-out — re-verify on-device after iPadOS updates), ink accepts pen+mouse pointers only, fingers move photos. All pad failures degrade silently (rule 12): failed uploads never block a submit, failed extraction leaves typing/Scribble untouched. SQL: `~/AIS-Data-Dashboard/db/migrate_13_evidence_pads.sql`.

---

## Identity / auth (current state as of v0.10 · 2026-05-28)

Both Apps Script projects and both Sheets run as **`admin.user@ais.ae`** (Super Admin). Single-identity setup across the whole system. R3 also requires the **AdminDirectory** advanced service (for the staff-groups teacher list).

**Pre-v0.9 history:** the lesson observation script was originally on `igorsbasketball@gmail.com` because an AIS Workspace policy blocked `ANYONE_ANONYMOUS` Apps Script Web Apps under @ais.ae. Migrated to admin.user once Super Admin rights landed; empirical test confirmed @ais.ae now serves anon web apps without a visible Admin Console toggle being flipped (likely Super Admin auto-trust for internal apps). The old igorsbasketball deployment is the rollback path, decommission target ~2026-06-07.

**Diagnosing future Apps Script web app 302s:** A 302 from `/macros/.../exec` is *normal*. Apps Script always sandbox-redirects to `script.googleusercontent.com/macros/echo?...` to serve responses. **Look at the `Location:` header** before panicking: `googleusercontent.com` = healthy, `accounts.google.com/ServiceLogin` = auth broken. For POSTs via `curl`, do NOT use `-X POST`. Apps Script needs the redirect followed as a GET (browser default behavior).

---

## Deploy chain

### HTML changes (`index.html` or either form)

1. Edit master, bump version labels everywhere (see hard rule 6). For R3 the master is `Assets/R3/src/r3-evidence-form.html`.
2. **R3 only:** run `Assets/R3/encrypt.sh`; it regenerates BOTH `Assets/R3/r3-evidence-form.html` (encrypted) and the ungated teacher viewer `Assets/R3/r3-record.html`. Lesson Observation is not encrypted; skip this step.
3. `git add -p && git commit -m "vX.Y · ..."`
4. `git tag vX.Y && git push origin main --tags`
5. **Pages deploys are self-healing now (migrated 2026-07-05, replaces the old branch build).** The Pages source is **GitHub Actions** (`build_type=workflow`); deploys run our own workflow `.github/workflows/pages.yml` (build job uploads once, deploy job retries the publish 3x to absorb GitHub's intermittent "Deployment failed, try again later" backend transient, `actions/deploy-pages` #406/#418, and `concurrency: cancel-in-progress:false` QUEUES deploys so the 2026-07-04 twin-build wedge can no longer happen). **To ship: just `git push origin HEAD:main`** and leave it; do NOT `POST pages/builds` (that legacy endpoint no longer drives deploys, and there is no twin-build to avoid). **Verify:** poll the run to `completed/success` (`gh run list --repo Rogerceaser21/Data-Representation --workflow "Deploy to GitHub Pages"`) then md5-compare the live `dashboard/index.html` vs local. **Force a redeploy** with no code change: `gh workflow run "Deploy to GitHub Pages" --ref main`. **Rollback** (only if the workflow ever misbehaves; the live site keeps serving throughout): `gh api -X PUT repos/Rogerceaser21/Data-Representation/pages -f build_type=legacy -f 'source[branch]=main' -f 'source[path]=/'`.
6. For large pushes (touching the docx template or full forms), Git push may fail with `HTTP 400`. Fix: `git config http.postBuffer 524288000` and retry.

### Apps Script changes (either `apps-script/*.gs` or `Assets/R3/apps-script/*.gs`)

1. Edit `.gs` files
2. `cd <correct apps-script folder> && clasp push --force` (clasp must be logged in as admin.user@ais.ae)
3. **Redeploy in place** (URL stays stable): `clasp redeploy <deployment-id> -d "vX.Y summary"`. The R3 deployment id is `AKfycbx3efKiQzs2MSwESEuNBCceXr5FqBCXuk1IgSzPFbOVgLSc3fvXy40e8V9lhw_KH0z2nQ`. If you must create a brand-new deployment (`clasp create-deployment ...`), update the matching `WEB_APP_URL` in the form HTML and re-encrypt R3.
4. **First-time deploys or new scopes:** open the script in the browser editor, run `forceAuth` (and `bootstrap` + `buildTeacherSheet` for R3), click through OAuth consent. CLI cannot trigger this.

### Primary checkout note (standing, 2026-07-03)

The main checkout at the project root is **intentionally stale** (June 2026 state, a diverged
local docs commit + a v0.34 WIP master copy, all superseded on `origin/main`). Igor's standing
call: **leave it; never flag it, never reset it.** Truth lives on `origin/main`; sessions build
and ship from `.claude/worktrees/*`; the primary checkout only serves the shared `handoff/` folder.

### Cache invalidation (R3 only, since v0.33)

`Assets/R3/apps-script/02_doGet.gs` `getDropdownOptions()` is wrapped in `CacheService` with a 5-minute TTL. If you edit the Teachers / Inspectors / Curriculum / Subjects tabs and need the form to see the change before the TTL expires:

1. Open the R3 Apps Script editor (link in `Assets/R3/Google file links.md`).
2. Pick `clearOptionsCache` from the function dropdown.
3. Run.

Otherwise the form serves the cached options until natural expiry.

---

## Coding discipline (Karpathy guidelines)

These apply to every edit in this repo. If a request seems to require violating one, surface it first.

1. **Think before coding.** State assumptions explicitly. If multiple interpretations exist, present them; don't pick silently. If a simpler approach exists, say so. Push back when warranted. If something's unclear, stop and ask.
2. **Simplicity first.** Minimum code that solves the problem. No speculative features, no abstractions for single-use code, no flexibility or configurability that wasn't requested, no error handling for impossible scenarios. If 200 lines could be 50, rewrite it.
3. **Surgical changes.** Touch only what the user asked about. Don't "improve" adjacent code, formatting, or comments. Match existing style even if you'd do it differently. Mention unrelated dead code; don't delete it. Every changed line should trace directly to the request.
4. **Goal-driven execution.** Transform vague tasks into verifiable goals before starting. "Add validation" becomes "Write tests for invalid inputs, then make them pass". For multi-step work, state a brief plan with a verify step per item.

---

## When in doubt

Ask Igor. He prefers a question over a wrong assumption, but the bar is *genuinely undetermined*. Things derivable from this repo, the sibling `Export Ready HTML AIS Report/`, or the prior conversation are not genuinely undetermined; infer the default and proceed (noting the assumption briefly).

Once Igor has weighed a concern and made a call, **execute**; don't re-raise the same concern with a different framing.

**Never use em dashes (—) or en dashes (–) in any output.** Substitute with commas, periods, parentheses, or semicolons. Hyphens inside compound words (like "spreadsheet-first" or "single-select") are fine.

---

## Related

| Thing | Value |
|---|---|
| This repo | `github.com/Rogerceaser21/Data-Representation` (private) |
| Live deck | https://rogerceaser21.github.io/Data-Representation/ |
| **Focus OS (task list)** ||
| Linked project | `AIS-Data-Dashboard` (id `4617bb5f-91da-4c14-a9dc-3b05aa0de972`) · holds this repo's task list; read/written by the `handoff_load` / `handoff_write` skills. The old `SPEA Dashboard` project was deleted 2026-06-23. |
| **Lesson Observation** ||
| Live form | https://rogerceaser21.github.io/Data-Representation/Assets/Lesson%20Observation%20Form/lesson-observation-form.html |
| Sample locked record | https://rogerceaser21.github.io/Data-Representation/Assets/Lesson%20Observation%20Form/lesson-observation-form.html?id=AIS-OBS-20241112-1430 |
| Sheet | https://docs.google.com/spreadsheets/d/1KKgHe42JnRe5iA8iODRrh3UZHD93szZSDatQylivoAc/edit |
| Apps Script | scriptId `1wfwv-3le3lMFeiV6E_hghRQ3F2kAiInNaNlnQbipx4bLMFH9N0yRKXEL` (admin.user) |
| Web App `/exec` | `https://script.google.com/macros/s/AKfycbw8b_yMyCg1cW63q4d6SDxOKRLeFFv7rwywCIAh-y4bY3ZUUxhKrpYOmkZXAODTeDI3/exec` |
| Seed record | `AIS-OBS-20241112-1430` (Ben Hyde observing Mr Igor, 2024-11-12) |
| **R3 Evidence** ||
| Live form (StatiCrypt-gated) | https://rogerceaser21.github.io/Data-Representation/Assets/R3/r3-evidence-form.html |
| Teacher viewer (ungated, record-link-only) | https://rogerceaser21.github.io/Data-Representation/Assets/R3/r3-record.html?id=...&token=... |
| Access password | `ais2026ais` (edit + re-encrypt to rotate) |
| Master source | `Assets/R3/src/r3-evidence-form.html` |
| Encrypt build | `Assets/R3/encrypt.sh` |
| Apps Script deployment id | `AKfycbx3efKiQzs2MSwESEuNBCceXr5FqBCXuk1IgSzPFbOVgLSc3fvXy40e8V9lhw_KH0z2nQ` (use with `clasp redeploy`) |
| Cache invalidation | run `clearOptionsCache` from the Apps Script editor |
| Google asset links | [`Assets/R3/Google file links.md`](Assets/R3/Google%20file%20links.md) |
| Sheet | https://docs.google.com/spreadsheets/d/1V1Nb8hTWN-FpDps2Q_-F0TBWBFnPHVBW_sicWk8XVKo/edit |
| Apps Script | scriptId `1BYxWpSsqs-48AKnWH4BUQonAKHqDcY6VbiWA1SNbhBZT0QgDzLUatgRn` (admin.user) |
| Web App `/exec` | `https://script.google.com/macros/s/AKfycbx3efKiQzs2MSwESEuNBCceXr5FqBCXuk1IgSzPFbOVgLSc3fvXy40e8V9lhw_KH0z2nQ/exec` |
| Backup email mailbox | `admin.user@ais.ae` · CC to selected inspector (from `Inspectors!B` column) · auto-label `R3 Submissions` via Gmail filter |
| Source template | `Assets/R3/R3 Evidence Form Template.docx` |
| **Lesson Observation rollback (pre-v0.9, decommission ~2026-06-07)** ||
| Apps Script | scriptId `1_7so0rIf1guEYc8AkEPj6f2-yDerR3aH4tItFYDemf7E_UdcrtbWky_e` (igorsbasketball@gmail.com) |
| Web App `/exec` | `https://script.google.com/macros/s/AKfycbzoxepKzA8vSQkDd0_OEl65EVw017BCC_kFhxSJueP5nZqTQYMtLVMuUr_vpILdavQ4/exec` |
| **Other** ||
| Sibling design-precedent | `../Export Ready HTML AIS Report/` (academic report, StatiCrypt, ship.sh workflow) |
| Sibling deployed | `github.com/Rogerceaser21/sample-student-report` |
